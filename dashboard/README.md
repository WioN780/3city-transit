# dashboard

React dashboard that will consume the Go API (`/api/v1/routes/worst-offenders`,
`/api/v1/hotspots`). Not scaffolded yet and not part of `docker-compose.yml` --
add a Vite/CRA app here and a `dashboard` service once there's UI to serve.
